{
  "name": "digital-asset-marketplace",
  "version": "1.0.0",
  "description": "Simple NFT marketplace using Ether — Hardhat + minimal frontend",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "node": "npx hardhat node",
    "deploy": "npx hardhat run --network localhost scripts/deploy.js"
  },
  "devDependencies": {
    "hardhat": "^2.12.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3",
    "ethers": "^5.7.2"
  }
}
